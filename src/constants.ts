export const FPS = 30;

export const TALK_GAP_FRAMES = 25;

export const SUBTITLE_HEIGHT_PX = 80;

export const DEFAULT_SECTION_INITIAL_DELAY_FRAMES = 30;

export const DEFAULT_SECTION_END_FRAMES = 30;

export const CHARACTER_WIDTH_PERCENTAGE = 0.15;

export const zIndex = {
  anyValue: 1,
  subtitle: 10,
  yukkuri: 10,
  transitionMovie: 10000,
} as const;
